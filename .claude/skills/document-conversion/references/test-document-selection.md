# Test document selection

Choose 1-2 representative documents for the orchestrator to test-migrate to Prismic. Pick documents that:

- Exercise the most fields (avoid sparse documents).
- Have rich text with varied content (headings, lists, images, links).
- Use relationships to other types.
- Cover edge cases if any exist.
- Explain why each was chosen.